[project]
name = "apm"
version = "0.1.0"
description = "Readonly Polymarket and Kalshi research screen"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
apm = "apm.cli:main"
